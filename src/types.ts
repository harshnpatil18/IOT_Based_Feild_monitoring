export interface SoilData {
  moisture: number;
  ph: number;
  temperature: number;
  humidity: number;
  timestamp: string;
}

export interface Practice {
  title: string;
  description: string;
  imageUrl: string;
  category: string;
}
